Portfolio+ contact form plugin
===

Settings
---
The only external setting the contact form plugin requires is the
`admin_email_address` which is set in the site-config.php file.

Usage
---
Add the contact form to a page or partial by instructing the Plugin manager to display it.

    <?php Plugins::display('ContactForm'); ?>
